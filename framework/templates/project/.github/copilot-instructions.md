<!--
NOT A TEMPLATE.

.github/copilot-instructions.md is regenerated by `make sync-agents`
(which runs `.github/scripts/sync-agents.sh`) from `_canonical.md`.
Copilot, Claude Code, and Codex all read self-contained per-agent files
rendered from the same canonical source.

Do not render this file into a scaffolded project — the bootstrap
workflow emits .github/copilot-instructions.md after `_canonical.md` is
in place.
-->
