<!--
NOT A TEMPLATE.

CLAUDE.md is regenerated by `make sync-agents` (which runs
`.github/scripts/sync-agents.sh`) from `_canonical.md`. Claude Code,
Codex, and Copilot all read self-contained per-agent files rendered from
the same canonical source.

Do not render this file into a scaffolded workspace — the bootstrap
workflow emits CLAUDE.md after `_canonical.md` is in place.
-->
