<!--
NOT A TEMPLATE.

AGENTS.md is regenerated by `make sync-agents` (which runs
`.github/scripts/sync-agents.sh`) from `_canonical.md`. Codex needs
self-contained content (no `@`-imports), which the script guarantees.

Do not render this file into a scaffolded workspace — the bootstrap
workflow emits AGENTS.md after `_canonical.md` is in place.
-->
