# Tool: GitHub Copilot CLI

## Instruction-file discovery path

**Assumed path:** `~/.copilot/AGENTS.md`

**Verification status:** UNVERIFIED — GitHub Copilot CLI (`gh copilot`) is not installed on
this host (`gh` not found). The `~/.copilot/` directory exists but is populated by JetBrains
and other IDE Copilot integrations, not by a standalone Copilot CLI tool.

**Verification method when tool is available:**
```
gh copilot --help | grep -i 'agents\|instruct\|global'
```
If the actual instruction-file path differs from `~/.copilot/AGENTS.md`, update only the
FR-9 `tool→target map` row in `scripts/ai-switch.sh` — that is the single point of change
per the spec.

**Note in spec (FR-4):** "if a future Copilot release reads from `~/.config/github-copilot/`,
the script's tool→target map is the single point of update."

**Target in FR-9 tool→target map:** `~/.copilot/AGENTS.md` (assumed; validate before first use).

## Rendered output

`ai-switch.sh` writes `~/.copilot/AGENTS.md` by rendering `AGENTS.md.template` via allowlisted
`envsubst`, appending the active profile's `AGENTS.md` body fragment (generated by `sync-agents.sh`
from `copilot-instructions.md`), then appending `preferences.md`.

## Skills overlay

`ai-switch.sh` creates per-entry symlinks under `~/.copilot/skills/` and `~/.copilot/agents/`.
Reference symlinks also created:
- `~/.copilot/spec-workflows` → `$AI_DOTFILES/framework/spec-workflows`
- `~/.copilot/prompts` → `$AI_DOTFILES/framework/prompts`
- `~/.copilot/templates` → `$AI_DOTFILES/framework/templates`
- `~/.copilot/boundaries.md` → `$AI_DOTFILES/framework/boundaries.md`
