# profiles/work.example/profile.env
# When populating this profile, add CLAUDE.md, copilot-instructions.md, then run sync-agents.sh to generate AGENTS.md.
#
# Backend vars (ANTHROPIC_BASE_URL, ANTHROPIC_AUTH_TOKEN, ANTHROPIC_API_KEY)
# belong in backends/*.env — never here (FR-7a).

# fill at work
# export AI_PROFILE="work"
# export ANTHROPIC_MODEL="claude-sonnet-4-6"
